.well
  %h4 Новости конкурса
  - @news.each do |news|
    %hr
    %a{:href => "/news/#{news.id}"}
      %h4= news.title
      %p
        != strip_tags news.body.first(200)
      %small= news.pusblished_at
