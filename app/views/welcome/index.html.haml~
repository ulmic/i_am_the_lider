= stylesheet_link_tag    "welcome", :media => "all"
.span3
  %h3 Проекты
  %table.table-striped
  -@projects.each do |project|
    %tr{:href => "/projects/#{project.id}"}
      %h5= project.title
      = project.description
      %br
      %img.img-circle.small{:src => project.user.avatar, :href => "/users/#{project.user.id}"}  
      %a{:href => "/users/#{project.user.id}"}
        = project.user.title
      %hr
