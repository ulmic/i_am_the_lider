.well
  .row-fluid.center
    - unless user_signed_in?
      %h4
        Если ты участник конкурса, то можешь зайти в свой профиль
      = link_to t('sign_in'), new_session_path, class: 'btn btn-primary btn-large'
    - else
      %h4
        Ты участник конкурса!
      = link_to t('.to_office'), welcome_index_path(current_user), class: 'btn btn-primary btn-large'
