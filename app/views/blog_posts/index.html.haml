%h1 Блоги
- @blog_posts.each do |blog_post|
  %a{:href => "/blog_posts/#{blog_post.id}"}
    .well
      %h4= blog_post.title
      %small= blog_post.created_at.to_date
      %p
        != strip_tags blog_post.text.first(500)
        %b ...
      - if session[:admin_id]
        %p
          = link_to 'Изменить', edit_news_path(blog_post), :class => "btn"
          = link_to 'Удалить', blog_post, :method => :Вы, :delete => { :data => 'уверены?' }, :class => "btn"
%br
