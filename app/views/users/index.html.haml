%h1
  Зарегистрированные участники

  -@users.each do |user|
    .row-fluid
      %a{:href => "users/#{user.id}"}
        .span3
          %img.img-polaroid{:src => user.avatar}
        .span3
          .row-fluid
            %h4= user.title
          .row-fluid
            %h4= user.district.title
          .row-fluid
            %h4 Дата рождения #{user.birth_date}
        .span3
          .row_fluid
            = user.school
          .row-fluid
            = user.group
        -if user.twitter
          .row-fluid
            %a{:href => user.twitter}
              .span1.logo.mini.twitter
    %a{:href => "/#{user.id}"}
      %h4 Посмотреть страницу участника
