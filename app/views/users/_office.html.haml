%p{:id => "notice"}
  = notice
= form_for @user, :url => 'logout' do |f|
  - if session[:user_id] == @user.id
    Hello,
    = @user.title
    = f.submit 'Log out'
  = simple_form_for @user do |f|
    .row-fluid
      .span5
        %p
          %h2= @user.title
        %p
          %img.img-polaroid.big_img{:src => @user.avatar} 
        %p
          %b= f.label :district
          = @user.district.title
        %p
          %b= f.label :birth_date
          = @user.birth_date
        %p
          %b= f.label :school
          = @user.school
        %p
          %b= f.label :group
          = @user.group
        %p
          %b= f.label :twitter
          = @user.twitter
      .span4
        %p
          %h3= @user.project.title
        %p
          = f.label :description
          = @user.project.description
        %p
          = f.label :goal
          = @user.project.goal
        %p
          = f.label :place
          = @user.project.place
        %p
          = f.label :target_audience
          = @user.project.target_audience
        %p
          = f.label :results
          = @user.project.results
      .span3
        %p
          %h3 Отчёт о мероприятии
        %p
          = f.label :member_count
          = @user.report.member_count
        %p
          = f.label :member_type
          = @user.report.member_type
        %p
          = f.label :venue
          = @user.report.venue
        %p
          %a{:href => "#{@user.report.archive}"} Скачать архив
